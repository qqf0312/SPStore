#include <libblockchain/BlockChainImp.h>
#include <libmptstate/Eurasure.h>
#include <libmptstate/MPTState.h>
class Client
{
public:
    Client(
        std::shared_ptr<dev::blockchain::BlockChainInterface> _blockchain_manager
        )
      : blockchain_manager(_blockchain_manager)
    {}
    bool initClient();
    void getState(std::string key);
private:
    std::shared_ptr<dev::blockchain::BlockChainInterface> blockchain_manager;
    std::shared_ptr<dev::mptstate::MPTState> m_mptstate;
};